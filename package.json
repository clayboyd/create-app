{
  "name": "createapp",
  "version": "1.0.0",
  "description": "- create-react-app client  - npx express-generator server  - (cd server; npm install)",
  "main": "index.js",
  "scripts": {
    "install": "(cd client; npm install) && (cd server; npm install)",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "(cd client; npm start &) && (cd server; export PORT=3100; npm start &)"
  },
  "author": "Clay Boyd",
  "license": "ISC"
}
